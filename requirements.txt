########################################
# Project requirements for final_CENG467
# - Python compatibility: tested with Python 3.8-3.11
# - Use bounded ranges to avoid accidental breaking upgrades
########################################

# Core data and ML utilities
pandas==1.5.3
numpy>=1.21,<2.0
scikit-learn>=1.0,<2.0

# EHRSQL / evaluation utilities
dask>=2022.0.0,<2024.0.0
func-timeout>=4.3.5
sqlparse>=0.4.4

# Transformer / text-to-SQL baselines
# Pin `transformers` for reproducibility; adjust `torch` for your CUDA
transformers==4.29.2
sentencepiece>=0.1.99
torch>=1.13.0,<2.0
accelerate>=0.20.0
datasets>=2.12.0

# Retrieval / embedding baselines
rank-bm25>=0.2.2
sentence-transformers>=2.2.2

# Experiment and notebook utilities
tqdm>=4.64.0
jupyter>=1.0.0
notebook>=6.5.0

# Optional / helper
typing_extensions>=4.0.0
protobuf>=3.20.0

# Notes:
# - If you need a specific CUDA build, install `torch` separately following
#   https://pytorch.org/get-started/locally/ and then re-run pip for the
#   remaining packages: `pip install -r requirements.txt --no-deps`.
